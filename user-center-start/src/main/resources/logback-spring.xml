<?xml version="1.0" encoding="UTF-8"?>
<configuration scan="false" scanPeriod="60 seconds" debug="false">
    <!--一些固定的属性初始化，如host，ip等-->
    <contextListener class="com.ravey.common.utils.log.LogPropertyInitListener"/>
    <!--日志产生的服务名-->
    <springProperty scope="context" name="appName" source="spring.application.name" defaultValue="app"/>
    <springProfile name="dev">
        <!--输出到控制台-->
        <appender name="console" class="ch.qos.logback.core.ConsoleAppender">
            <encoder>
                <pattern>%d{HH:mm:ss} [%X{traceId}] [%thread] %-5level %logger{36} - %msg%n</pattern>
            </encoder>
        </appender>
        <root level="info">
            <appender-ref ref="console"/>
        </root>
    </springProfile>
    <springProfile name="prod,pre,test,debug">
        <springProperty scope="local" name="logback.logDir" source="logs.dir" defaultValue="./logs"/>
        <contextName>${appName}</contextName>
        <!--按天生成日志-->
        <appender name="fileErrorLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <!--日志名称，如果没有File 属性，那么只会使用FileNamePattern的文件路径规则
                如果同时有<File>和<FileNamePattern>，那么当天日志是<File>，明天会自动把今天
                的日志改名为今天的日期。即，<File> 的日志都是当天的。
            -->
            <filter class="ch.qos.logback.classic.filter.ThresholdFilter">
                <level>ERROR</level>
            </filter>
            <File>${logback.logDir}/error.${appName}.log</File>
            <!--滚动策略，按照时间滚动 TimeBasedRollingPolicy-->
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <!--文件路径,定义了日志的切分方式——把每一天的日志归档到一个文件中,以防止日志填满整个磁盘空间-->
                <FileNamePattern>${logback.logDir}/history/error.${appName}.%d{yyyy-MM-dd}.%i.log</FileNamePattern>
                <maxFileSize>1GB</maxFileSize>
                <!--日志保留天数-->
                <maxHistory>5</maxHistory>
                <!--用来指定日志文件的上限大小，那么到了这个值，就会删除旧的日志-->
                <totalSizeCap>20GB</totalSizeCap>
            </rollingPolicy>
            <!--日志输出编码格式化-->
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <pattern>
                        <pattern>
                            {
                            "time": "%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Shanghai}",
                            "level": "%level",
                            "app": "${appName}",
                            "traceId": "%X{traceId}",
                            "remoteIp": "%X{remoteIp}",
                            "host": "${host}",
                            "thread": "%thread",
                            "logger": "%logger{35}",
                            "message": "%message",
                            "stacktrace": "%exception{10}"
                            }
                        </pattern>
                    </pattern>
                </providers>
            </encoder>
        </appender>
        <appender name="fileInfoLog" class="ch.qos.logback.core.rolling.RollingFileAppender">
            <filter class="ch.qos.logback.classic.filter.LevelFilter">
                <level>ERROR</level>
                <onMatch>DENY</onMatch>
                <onMismatch>ACCEPT</onMismatch>
            </filter>
            <File>${logback.logDir}/info.${appName}.log</File>
            <rollingPolicy class="ch.qos.logback.core.rolling.SizeAndTimeBasedRollingPolicy">
                <FileNamePattern>${logback.logDir}/history/info.${appName}.%d{yyyy-MM-dd}.%i.log</FileNamePattern>
                <maxFileSize>1GB</maxFileSize>
                <!--日志保留天数-->
                <maxHistory>5</maxHistory>
                <!--用来指定日志文件的上限大小，那么到了这个值，就会删除旧的日志-->
                <totalSizeCap>30GB</totalSizeCap>
            </rollingPolicy>
            <encoder class="net.logstash.logback.encoder.LoggingEventCompositeJsonEncoder">
                <providers>
                    <pattern>
                        <pattern>
                            {
                            "time": "%d{yyyy-MM-dd HH:mm:ss.SSS, Asia/Shanghai}",
                            "level": "%level",
                            "app": "${appName}",
                            "traceId": "%X{traceId}",
                            "remoteIp": "%X{remoteIp}",
                            "host": "${host}",
                            "thread": "%thread",
                            "logger": "%logger{35}",
                            "message": "%message",
                            "stacktrace": "%exception{10}"
                            }
                        </pattern>
                    </pattern>
                </providers>
            </encoder>
        </appender>
        <root level="info">
            <appender-ref ref="fileInfoLog"/>
            <appender-ref ref="fileErrorLog"/>
        </root>
    </springProfile>
</configuration>